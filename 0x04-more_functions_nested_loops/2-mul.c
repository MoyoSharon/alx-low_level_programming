#include "main.h"
/**
 *  * mul - multiplies two integers
 *   * @a: integer to multiply
 *   @b: integer to multiply
 *    *
 *     * Return: 0
 */
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
